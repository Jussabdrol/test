version: '3.8'

# Local development with containerized app (connects to Supabase)
# Usage:
#   docker-compose up       # Start app
#   docker-compose down     # Stop app

services:
  app:
    build: .
    container_name: lettheframework-app
    env_file:
      - .env
    ports:
      - "3000:3000"
