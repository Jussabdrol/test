# Let The Framework - Environment Configuration
# Copy this file to .env and adjust values for your environment

# =============================================================================
# Supabase Configuration
# =============================================================================

# Supabase project URL & anon key (used for Storage)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase PostgreSQL connection string (pooler recommended for serverless)
# Find this in Supabase Dashboard → Settings → Database → Connection string → URI
SUPABASE_DB_URL=postgresql://postgres.[ref]:[password]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# Or provide individual connection pieces instead of SUPABASE_DB_URL:
# SUPABASE_DB_HOST=aws-0-eu-central-1.pooler.supabase.com
# SUPABASE_DB_PORT=6543
# SUPABASE_DB_USER=postgres.[ref]
# SUPABASE_DB_PASSWORD=your-password
# SUPABASE_DB_NAME=postgres

# Connection pool size (default 10, lower for serverless)
# DB_POOL_MAX=5

# =============================================================================
# Server Configuration
# =============================================================================

# Port to run the server on
PORT=3000

# Secret used to sign session tokens (change in production!)
SESSION_SECRET=change-me-to-a-random-string

# =============================================================================
# Quick Start
# =============================================================================
#
# 1. Create a Supabase project at https://supabase.com
# 2. Run the SQL migration in supabase-migration.sql via the SQL Editor
# 3. Create a storage bucket named "uploads" (Settings → Storage)
# 4. Copy this file to .env and fill in the values above
# 5. npm install && npm start
